# Kalakal Agent — environment variable template.
# Slice 1 (project scaffold) requires no runtime environment variables.
# Future slices will document their variables here as placeholders only.
# Never put secrets, keys, or credentials in this file or in the repository.
